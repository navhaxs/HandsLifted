<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
			 xmlns:prepare="using:HandsLiftedApp.Views.Prepare"
 	 		 xmlns:material="using:Material.Icons.Avalonia"
             mc:Ignorable="d" d:DesignWidth="700" d:DesignHeight="80"
             x:Class="HandsLiftedApp.Views.Prepare.PrepareItem">
	<Border
		Padding="8"
		Background="#f6f6fc"
		CornerRadius="6"
		Margin="0 0 0 6">
	<DockPanel>
		<!--<material:MaterialIcon Kind="ApplicationOutline"
		                       DockPanel.Dock="Left"
		                       Margin="10"
		                       MinHeight="40"
		                       MinWidth="40" 
		                       Foreground="#909292"
		                       />-->
		<Button Content="Set" DockPanel.Dock="Right" MinWidth="80" VerticalAlignment="Center" />
		<TextBlock Text="{Binding ItemTitle, FallbackValue='Untitled Item'}"
		           FontSize="14"
		           VerticalAlignment="Center"
		           />
	</DockPanel>
	<!--EnableSlideEvents="{Binding $parent[viewsrender:SlideRenderer].IsLive}"
	/>-->
	</Border>
</UserControl>

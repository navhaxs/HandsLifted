<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:HandsLiftedApp.Core.ViewModels"
             xmlns:controls="clr-namespace:HandsLiftedApp.Controls;assembly=HandsLiftedApp.Controls"
             xmlns:core="clr-namespace:HandsLiftedApp.Core"
             xmlns:controls1="clr-namespace:HandsLiftedApp.Controls"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             x:Class="HandsLiftedApp.Core.Views.MainView"
             Background="Yellow"
             x:DataType="vm:MainViewModel">
    <Design.DataContext>
    <vm:MainViewModel />
  </Design.DataContext>

    <Grid>
	    
	    <Grid.ColumnDefinitions>
		    <ColumnDefinition Width="*" MaxWidth="400" />
		    <ColumnDefinition Width="6" />
		    <ColumnDefinition Width="2*" />
	    </Grid.ColumnDefinitions>

	    <DockPanel>
	        <TextBox
		        Text="{Binding CurrentPlaylist.Playlist.Title, FallbackValue='Untitled Playlist'}" 
				Padding="22 16"
				FontSize="16"
				HorizontalAlignment="Stretch"
		        BorderThickness="0"
				VerticalAlignment="Center" 
				DockPanel.Dock="Top"/>
            <UniformGrid Rows="1" DockPanel.Dock="Top">
                <Button Click="OpenFileButton_Clicked" HorizontalAlignment="Stretch">Open file</Button>
                <Button Click="SaveFileButton_Clicked" HorizontalAlignment="Stretch">Save file</Button>
                <Button Click="TestButton_Clicked" HorizontalAlignment="Stretch">Info</Button>
			</UniformGrid>
			<controls:ItemOrderListView DataContext="{Binding CurrentPlaylist}"
										DockPanel.Dock="Left"/>

		</DockPanel>

		<GridSplitter Grid.Column="1" Background="Red" />

		<DockPanel Grid.Column="2">
            <StackPanel Background="Aquamarine">
                <DockPanel>
                    <ListBox
                        DockPanel.Dock="Left"
                        ItemsSource="{Binding CurrentPlaylist.Playlist.Items}"
                        SelectedIndex="{Binding CurrentPlaylist.SelectedItemIndex}"
                        />
                    <StackPanel>
                        <core:SongLyricEditor DataContext="{Binding CurrentPlaylist.SelectedItem}" />
                        <controls1:ItemSlidesView DataContext="{Binding CurrentPlaylist.SelectedItem}" />
                    </StackPanel>
                </DockPanel>
            </StackPanel>
		</DockPanel>

    </Grid>
</UserControl>
